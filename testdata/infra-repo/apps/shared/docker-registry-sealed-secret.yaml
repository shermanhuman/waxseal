apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: docker-registry-creds
  namespace: default
  annotations:
    sealedsecrets.bitnami.com/scope: namespace-wide
spec:
  encryptedData:
    .dockerconfigjson: AgFX7C4fK9FAKE_ENCRYPTED_DOCKER_CONFIG_JSON...
  template:
    type: kubernetes.io/dockerconfigjson
    metadata:
      name: docker-registry-creds
      namespace: default
